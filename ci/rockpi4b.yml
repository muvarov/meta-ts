header:
  version: 9
  includes:
    - base.yml
    - meta-openembedded.yml

machine: rockpi4b

local_conf_header:
  bugs: |
    # Only ping until errors can be resolved
    TEST_SUITES = "ping"

target:
  - u-boot
