header:
  version: 9
  includes:
    - base.yml
    - meta-openembedded.yml

machine: stm32mp157c-dk2

local_conf_header:
  bugs: |
    # Only ping until errors can be resolved
    TEST_SUITES = "ping"
